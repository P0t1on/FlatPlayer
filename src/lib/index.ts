// place files you want to import through the `$lib` alias in this folder.
import { createKeyPressEvent } from "./Input";
import { getJson, getSource } from "./curl";

export { createKeyPressEvent, getJson, getSource };
